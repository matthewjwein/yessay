
header#header
  .navbar.navbar-inverse.navbar-fixed-top(role='navigation')
    .navbar-inner
      .container
        a.brand(href='/') Yessay
        .nav-collapse.collapse
          ul.nav.pull-right
            if (req.isAuthenticated())
              li
                a(href="/logout", title="logout")=req.user.username